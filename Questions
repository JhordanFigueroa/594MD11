1. FluTweet coordinates for JSON not working
2. Best way to match coordinates from States with actual State coordinates to figure out state of tweet
--Which data structure
  --returning the coordinates
3. Logger
4. Case senstive - FlUE

double tweetLatitude = Double.parseDouble(coordinatesSplit.substring(1, 18));
                    double tweetLongitude = Double.parseDouble(coordinatesSplit.substring(20, 39));


                    Double tweetCoordinates = tw.getLatitude();